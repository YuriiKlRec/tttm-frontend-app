import type { FC } from 'react'
import { TonConnectButton } from '@tonconnect/ui-react'

/** Кнопка підключення гаманця — реальний TonConnectButton. */
export const WalletButton: FC = () => <TonConnectButton />
