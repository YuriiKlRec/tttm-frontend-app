/** DEV-мок цін для сторінки оплати, коли корзина порожня (огляд верстки). */

/**
 * ~16 заброньованих цін — достатньо, щоб утворилось 2 чеки (по 8) і показався
 * слайдер із пагінацією. Значення навмисно різні для наочності.
 */
export const MOCK_PRICES: number[] = [
  65000, 65250.5, 64800, 66000, 63500.75, 67000, 64200, 65900,
  68000, 62500, 69000.25, 61000, 70000, 63000, 66500, 64000,
]

/** Ціна одного квитка в TON (як у моку гри). */
export const MOCK_TICKET_PRICE = '0.1'

/**
 * Ціни, що приходять «зайнятими» від бекенда (демо стану taken без оплати).
 * Має збігатися з однією з MOCK_PRICES.
 */
export const MOCK_TAKEN: number[] = [64800]
