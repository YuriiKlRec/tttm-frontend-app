/** Мок-дані екрана профілю (онбординг). */

/** Топ-гравець для верхньої мітки на ProfilePage. */
export const topPlayerNickname = '@webwest'

/**
 * Імітація зайнятих ніків (без мережі). Зберігаються у lowercase без `@`.
 * Якщо введений нік є у списку — вважаємо його зайнятим.
 */
export const takenNicknames: readonly string[] = ['somebody', 'webwest', 'admin']
