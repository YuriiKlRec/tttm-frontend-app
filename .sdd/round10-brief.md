# Раунд 10 — галочка мови, бордер форми, z-index плашки BTC, баг «@» у ніку

Репозиторій: `/Users/yuriikl/Code/its/Reack-and-Next/tttm/frontend-1.0` — React 19 + Vite + TS strict + Tailwind v4, працюєш прямо в main. Dev: `npm run dev -- --port 3120` (може вже крутитись). Тексти — тільки через i18n. Кольори — токени. Растр з Figma — тільки з прозорим фоном (перевірка альфи кутів); прості гліфи — інлайн-SVG.

Figma файл `sjYaf8Zax1vT2STG6pJPMd`.

## 1. Редагування профілю (вузол `1907:23294`) — два пропуски минулого раунду
Файли: EditProfilePage + компонент списку мов (з раунду 9, коміт ba84ad8).
- **Галочка у вибраній мові**: у макеті вибраний пункт списку має галочку (праворуч). Витягни точний гліф з вузла (інлайн-SVG), постав за макетом. Для a11y — aria-selected/aria-checked уже мають бути на пунктах; галочка декоративна (aria-hidden).
- **Бордер між списком мов і полем нікнейма**: у макеті між секцією ніка і секцією мов є розділювач-бордер — звір з design context (колір/стиль/відступи) і додай.

## 2. Waiting — z-index плашки BTC (баг: гра налазить на плашку)
Дослівно: «плашка з курсом біткоїна має менший зет індекс чим гра тому налазить гра на плашку».
- При скролі картки ігор малюються ПОВЕРХ закріпленої плашки курсу. Плашка (і, перевір, StatusLine/TopBar теж) мають бути НАД контентом, що скролиться: дай закріпленому блоку z-index (у стилі проєкту, напр. z-10; звір як зроблено BottomNav) і перевір усі фіксовані елементи AppLayout на цю ж проблему.
- Скрол-тест: проскроль waiting зі справжніми/стабнутими іграми — жоден піксель картки не перекриває плашку (скріншот у момент часткового перекриття зони).

## 3. Баг «@» у полі ніка при прямому відкритті /edit-profile (знайдений у раунді 9, pre-existing)
Симптом: при прямому переході на /edit-profile поле ніка показує лише «@» — `useNicknameInput` захоплює user до готовності auth.
- Це задача СИСТЕМАТИЧНОГО НАЛАГОДЖЕННЯ: відтвори (прямий goto на /edit-profile, повільний auth — за потреби стаб /api/me із затримкою), знайди точний механізм, зроби мінімальний правильний фікс (наприклад, синхронізація значення після ready або ініціалізація поля з user коли він з'являється — але НЕ затирати те, що юзер уже почав вводити).
- Доведи: прямий перехід показує нік; перехід зі сторінки профілю — показує нік; редагування під час повільного auth не затирається.

## Верифікація
- Playwright 390×844 порт 3120: скріншоти списку мов проти Figma-вузла; скрол-тест п.2; сценарії п.3.
- 3 мови, tsc чистий, build зелений, lint не гірше базлайну (8 старих errors).
- Коміти: англійська, імператив, ≤70 символів.

## Звіт
Повний: `/Users/yuriikl/Code/its/Reack-and-Next/tttm/frontend-1.0/.sdd/round10-report.md`.
Фінал: статус, коміти, 1 рядок tsc/build/lint, механізм бага п.3 одним реченням, concerns.
