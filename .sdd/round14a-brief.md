# Раунд 14A — плашка дати до лінії + GRAM скрізь

Репозиторій: `/Users/yuriikl/Code/its/Reack-and-Next/tttm/frontend-1.0` (React 19 + Vite + TS strict + Tailwind v4), main.
**git commit НЕ робиш** (паралельний агент у графіку; коміти зробить оркестратор).
**Твоя зона:** GameHeader/GamePage-хром, будь-які файли з відображенням валюти (BetPanel, buy-флоу, results, details, profile, i18n-ключі). НЕ чіпай src/components/games/chart/* і useChartData/binance — там паралельний агент.
Dev: `npm run dev -- --port 3126`. Playwright 390×844. Стаби page.route (dto: src/services/dto/, тікети ownerId).

## 1. Плашка «дата+таймер» відірвана від лінії знизу
Дослівно: «Плашка дата і таймер відірвана від лінії знизу.» Це продовження фікса B4 раунду 13 (stretch не доробив): плашка з датою/таймером угорі праворуч (GameHeader) має ДОТИКАТИСЬ вертикальної лінії, що йде під нею — зараз між низом плашки і лінією розрив. Відтвори (звичайна і довга назва гри — стаб), знайди причину (рамка плашки закінчується раніше за ряд? лінія починається нижче?), зроби так, щоб плашка і лінія були візуально з'єднані на будь-якій висоті ряду. Скріншоти до/після з короткою і довгою назвою.

## 2. GRAM замість TON у ВСІХ видимих місцях
Дослівно: «думаю GRAM скрізь треба замінити передивись код і заміни». 
- Знайди ВСІ місця, де користувачу показується «TON» як валюта сум (grep: 'TON' у tsx/ts, i18n-ключі у backend/src/i18n/translations.seed.ts зі словом TON, форматери utils/units). Приклади: «= 0.1 TON» на екрані гри (BetPanel/панель ставки), buy-флоу (Total/Pay), results, create-game, profile history.
- Заміни на GRAM: через наявний `formatAmount` (units.ts, дефолт GRAM) або оновлення i18n-значень (у seed міняй ЗНАЧЕННЯ ключів; сидер не апдейтить існуючі — перелічи змінені ключі у звіті, оркестратор оновить БД).
- НЕ чіпай: технічні/блокчейн місця — TonConnect UI, назви мережі, адреси, комісії мережі в TON якщо такі є (це реальна мережа TON!), змінні/типи в коді (nanoTON тощо — лише ВІДОБРАЖЕННЯ валюти ставок/призів міняється).
- Склади у звіті таблицю: файл/ключ → було → стало. Пройди всі екрани (гра, buy, results, create, profile) у Playwright і переконайся що «TON» ніде не лишилось у сумах (окрім легітимних TonConnect/мережевих місць — перелічи їх окремо як свідомо залишені).

## Верифікація
- Скріншоти екранів зі зміненими сумами; п.1 — до/після з двома довжинами назв.
- 3 мови; tsc чистий; build зелений; lint не гірше базлайну (7 errors після р.13).

## Звіт
`/Users/yuriikl/Code/its/Reack-and-Next/tttm/frontend-1.0/.sdd/round14a-report.md`.
Фінал: статус, СПИСОК ЗМІНЕНИХ ФАЙЛІВ (frontend + backend seed окремо, без комітів), змінені i18n-ключі, 1 рядок tsc/build/lint, concerns.
