# Раунд 7 — три зміни UI у frontend-1.0 (TTTM Mini App)

Репозиторій: `/Users/yuriikl/Code/its/Reack-and-Next/tttm/frontend-1.0` — React 19 + Vite + TypeScript strict + Tailwind v4. Працюєш прямо в main. Dev: `npm run dev -- --port 3120`. Дані поки з моків (`src/mocks/`), i18n вже є (3 мови — усі видимі тексти через переклади).

Джерело правди — Figma файл **`sjYaf8Zax1vT2STG6pJPMd`** (TTTM, дизайн застосунку). Використовуй get_design_context + get_screenshot + download_assets. Усе точно за макетом: розміри, кольори, відступи. Кольори — через наявні Tailwind-токени проєкту (подивись theme), нові сирі хекси не хардкодь без потреби (якщо токена нема — додай токен).

## Зміна 1 — Плашка «users online» (вузол `1360:11010`)
Файл: `src/components/layout/StatusLine.tsx`.
1. Квадратик-індикатор має **пульсувати**: плавно з'являтись і зникати (opacity fade in/out, CSS-анімація, нескінченний цикл; підбери тривалість природно, ~1–1.5s). Поважай `prefers-reduced-motion: reduce` — без анімації.
2. Додати **фон плашки як у макеті** (вузол 1360:11010) — звір заливку/прозорість/бордер/радіус з design context; якщо там растровий/векторний фон — витягни асет.

## Зміна 2 — Шапка (вузол `1251:3758`)
Файл: `src/components/layout/TopBar.tsx` (+ `src/components/icons/` за потреби).
1. **Фон** шапки — за макетом.
2. **Іконка перед логіном** — у макеті перед нікнеймом/логіном юзера є іконка; додай точну з Figma.
3. **Кольори** — звір усі кольори шапки з макетом (текст логіна, лого тощо) і приведи у відповідність.
4. **Кнопка додавання гри**: іконка в ній зараз завелика — заміни на точну з макета (розмір і сам гліф звір з вузлом 1251:3758).

## Зміна 3 — Основна частина: прибрати два елементи
1. **Пояснювальний текст під заголовком** — знайди на головних сторінках (PredictionsPage / WaitingPage / ResultsPage — де є) підзаголовок-пояснення під заголовком сторінки і видали його. Видали і його ключі з файлів перекладів (усі мови синхронно), якщо більше ніде не використовуються.
2. **Валютну пару з іконкою** — компонент `src/components/games/CurrencyPricePlate.tsx` (використовується у WaitingPage). Прибери його з рендеру; якщо компонент після цього ніде не використовується — видали файл і мертві імпорти/ключі перекладів.
УВАГА: видаляй тільки ці два елементи, нічого більше. Якщо не впевнений, який саме текст «пояснювальний» — подивись макет головного екрана в цьому ж Figma-файлі (там його під заголовком нема) і зістав.

## Верифікація
- Playwright MCP на 390×844 (це Telegram Mini App — мобільний в'юпорт основний): скріншоти плашки і шапки проти скріншотів Figma-вузлів; пульсацію перевір знявши 2 скріншоти з інтервалом ~700ms (opacity квадратика має відрізнятись).
- Перевір усі 3 мови — нічого не зламалось після видалення ключів.
- `npm run lint` (0 errors), тести проєкту якщо є (`npx vitest run`), `npx tsc -b` або build (`npm run build`) — зелені.
- Коміти: англійська, імператив, ≤70 символів, логічними групами (можна 3 окремі по зміні).

## Звіт
Повний звіт: `/Users/yuriikl/Code/its/Reack-and-Next/tttm/frontend-1.0/.sdd/round7-report.md` (що зроблено по кожній з 3 змін, чим верифіковано, точні значення з Figma).
У фінальному повідомленні: статус (DONE/DONE_WITH_CONCERNS/NEEDS_CONTEXT/BLOCKED), коміти, 1 рядок lint/test/build, concerns.
