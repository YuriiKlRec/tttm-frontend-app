# Раунд 14B — плавний перехід таймфреймів графіка (без ривка)

Репозиторій: `/Users/yuriikl/Code/its/Reack-and-Next/tttm/frontend-1.0` (React 19 + Vite + TS strict), main.
**git commit НЕ робиш** (паралельний агент; коміти зробить оркестратор).
**Твоя зона:** src/components/games/chart/*, src/hooks/useChartData.ts, src/services/binance.ts. Більше нічого.
Dev: `npm run dev -- --port 3127`. Playwright 390×844.

## Баг (дослівно від користувача)
«Графік дуже різко перестроюється коли доходить до нового проміжку, коротше з цим треба щось робити, як в прикладі віджеті зроблено — там один таймлайн? чи як, бо там плавно, а у нас дуже змінюється і виходить різкий ривок.»

## Контекст
Раунд 13A зробив передзавантаження всіх таймфреймів і атомарний своп зі збереженням часового вікна — але сам МОМЕНТ перемикання гранулярності (1m→5m→…) все одно дає видимий ривок: крива перемальовується з іншої кількості точок/іншою деталізацією стрибком.

## Задача — СИСТЕМАТИЧНО
1. **Вивчи еталон**: `/Users/yuriikl/Code/its/Reack-and-Next/tttm/bitcoin-price-widget` — як там влаштований таймлайн: один безперервний ряд даних з рендером за видимим діапазоном (без дискретного «перемикання» наборів)? інтерполяція/анімація переходу? гістерезис порогів? Опиши знайдений механізм у звіті.
2. **Перенеси підхід**: мета — під час зум-аута/свайпу перехід між гранулярностями ВІЗУАЛЬНО непомітний. Ймовірні шляхи (обери за еталоном): (а) один злитий таймлайн: рендерити з найдрібнішої доступної гранулярності агрегуючи на льоту під поточний масштаб (downsample у рендері замість підміни масиву); (б) кросфейд/твін між старою і новою кривою ~150-250мс; (в) гістерезис + перемикання лише коли розбіжність кривих субпіксельна. Якщо у віджеті «один таймлайн» — роби (а), це усуває клас проблеми.
3. **Доведи покадрово**: повільний безперервний зум-аут через 2-3 пороги гранулярності — лог максимального піксельного зсуву кривої між сусідніми кадрами в момент переходу (має бути ≈ як між звичайними кадрами зуму, без піка); візуально — запис серії скріншотів.
4. Не зламай зроблене в 13A: тап-контрол, кламп ≥0, кліп, прилипання до краю, min-height, збереження вікна при свайпі в історію. Прожени ці перевірки повторно.

## Верифікація
- Покадровий лог переходів (див. вище) на 390×844; регресія 13A-пунктів; tsc чистий; build зелений; lint не гірше базлайну (7 errors).

## Звіт
`/Users/yuriikl/Code/its/Reack-and-Next/tttm/frontend-1.0/.sdd/round14b-report.md`.
Фінал: статус, СПИСОК ЗМІНЕНИХ ФАЙЛІВ (без комітів), механізм еталона і обраний підхід одним-двома реченнями, 1 рядок tsc/build/lint, concerns.
