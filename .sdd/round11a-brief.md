# Раунд 11A — сторінка гри: ставки, деталі, кнопка копіювання

Репозиторій: `/Users/yuriikl/Code/its/Reack-and-Next/tttm/frontend-1.0` (React 19 + Vite + TS strict + Tailwind v4), main.
**НЕ РОБИ git commit** — паралельно працює інший агент; коміти зробить оркестратор. Просто зміни файли.
**Твоя зона файлів:** src/components/games/*, src/pages/GamePage.tsx, src/services/mappers.ts, src/types/*. НЕ чіпай CreateGamePage/create-game/* і backend — там працює інший агент. Якщо потрібен новий i18n-ключ — запиши які саме у звіт/concerns, НЕ редагуй backend/src/i18n/translations.seed.ts.
Dev: `npm run dev -- --port 3121`. Playwright 390×844. Figma файл `sjYaf8Zax1vT2STG6pJPMd`. Стаби API — через page.route (контракти src/services/dto/, у тікетах поле ownerId; приклади стабів — .sdd/round8-report.md).

## 1. Екран ставок гри (вузол `1360:11439`)
Дослівно: «там де Tickets кількість червона іконка це має бути не загальна а сума ставок інших гравців, і сховати стрілочки для сортування поки».
- Лічильник біля червоної іконки квитка: зараз загальна кількість тікетів — має бути кількість тікетів ІНШИХ гравців (усі мінус мої). Знайди де рахується (PredictionsView/мапер) і виправ; звір з вузлом що саме показує макет.
- Стрілочки сортування: приховати (не видаляти логіку — сховати UI «поки»; лиши компонент/код, прибери з рендеру або прапорцем).

## 2. Екран результату гри
Дослівно: «коли цей екран уже показує результат гри то не виділяти переможця оранжевим кольором тексту лише лишити іконку корони».
- У списку ставок на результатах: рядок переможця зараз оранжевим текстом — зробити звичайним кольором тексту, корона лишається.
- УВАГА: не зачепи виділення ВЛАСНОГО рядка користувача, якщо таке є окремо (звір з макетом результатів у цьому ж файлі — знайди фрейм).

## 3. Деталі гри (вкладка/блок Details на сторінці гри)
Дослівно: «щоб дата не переносилась на новий рядок і додати розділювачі по горизонталі як це уже є в деяких місцях на цій сторінці».
- Значення дати в рядку деталей не повинно переноситись (whitespace-nowrap + за потреби стискати лейбл; перевір 3 мови на 320/390 — БЕЗ обрізань: element.scrollWidth ≤ clientWidth, не ховай оверфлоу).
- Горизонтальні розділювачі між рядками деталей — таким самим стилем, як уже є на цій сторінці (знайди існуючий патерн розділювача і переюзай).

## 4. Кнопка копіювання посилання в плашці гри (кнопка: вузол `1251:3745`; галочка: вузол `1684:18587`)
Дослівно: «при кліку показати що щось відбулось, тому треба на 3 секунди показати іншу іконку галочку».
- Після кліку іконка міняється на галочку з вузла 1684:18587 (інлайн-SVG; якщо растр — прозорий фон, перевір альфу) на 3 секунди, потім назад. Повторний клік перезапускає таймер. Таймер чистити при unmount. a11y: aria-live «скопійовано» або зміна aria-label.
- Звір вигляд обох станів з вузлами.

## Верифікація
- Скріншоти проти Figma-вузлів; стани зі стабами (гра активна з чужими+моїми тікетами; гра фіналізована з переможцем; я-автор для кнопки копіювання).
- 3 мови, tsc чистий, build зелений, lint не гірше базлайну (8 старих errors).

## Звіт
`/Users/yuriikl/Code/its/Reack-and-Next/tttm/frontend-1.0/.sdd/round11a-report.md`.
Фінал: статус, СПИСОК ЗМІНЕНИХ ФАЙЛІВ (без комітів), 1 рядок tsc/build/lint, потрібні i18n-ключі якщо є, concerns.
