# Раунд 9 — лідерборд, профіль, редагування профілю, waiting-плашка

Репозиторій: `/Users/yuriikl/Code/its/Reack-and-Next/tttm/frontend-1.0` — React 19 + Vite + TS strict + Tailwind v4, працюєш прямо в main. Dev: `npm run dev -- --port 3120` (можливо вже запущений). i18n: всі видимі тексти ТІЛЬКИ через переклади (useT); ключі сервляться бекендом — новий ключ додавай так, як влаштовано в src/i18n/ (подивись механізм; якщо ключі сидяться з backend/src/i18n/translations.seed.ts — додай ключ туди значеннями 3 мов, це дозволено для цієї задачі, але БЕЗ коміту в backend-репо: лише зміни файлів, про коміт напиши в concerns).

Джерело правди: Figma файл **`sjYaf8Zax1vT2STG6pJPMd`**. Для КОЖНОЇ зміни: get_design_context + get_screenshot вузла, точні розміри/кольори/типографіка. Кольори — токени проєкту. Растр з Figma — ТІЛЬКИ з прозорим фоном (перевір альфу кутів попіксельно; sharp є у `/Users/yuriikl/Code/its/Reack-and-Next/tttm/website/node_modules/sharp`); прості гліфи — краще інлайн-SVG.

## 1. Лідерборд (вузол `1655:18169`) — LeaderboardPage
- **Додати заголовок** сторінки як у макеті (текст з макета → ключі перекладів 3 мов).
- **Прибрати букву «w»** біля кількості перемог у рядках лідерборду (в макеті число без суфікса).
- **Емті-стейт** (вузол `1655:18191`): перевір, чи реалізований порожній стан як у макеті; якщо ні/нема — зроби за макетом (текст → переклади). Верифікуй, підмінивши відповідь API порожнім списком через page.route у Playwright.

## 2. Профіль — іконка Win (вузол `1655:17996`) — ProfilePage
- Замінити поточну іконку «Win» на точну з макета (витягни асет з вузла; прозорий фон обовʼязково).

## 3. Редагування профілю (вузол `1907:23294`) — EditProfilePage
- **Вибір мови — списком** як на макеті (зараз інший UI — подивись поточну реалізацію): верстка списку, стани вибраного пункту, точно за вузлом. Доступність: role/aria для списку вибору, керування клавіатурою.
- **Порівняй верхню частину форми** з макетом (поля, підписи, відступи, порядок) і приведи розбіжності у відповідність. Перелічи знайдені розбіжності в звіті.

## 4. Waiting — плашка курсу біткоїна
- На сторінці Waiting плашка курсу біткоїна має бути **закріплена (sticky/fixed) так само, як плашка «онлайн»** (StatusLine — вона фіксована зверху в AppLayout, не скролиться) **і мати той самий фон** (#212121 = bg-background, суцільний).
- УВАГА на нещодавній фікс: PixelGrid малюється поверх фонів статичних елементів — закріплена плашка мусить мати `relative`/позиціонування, щоб фон був суцільним без сітки (див. коміт 404214d, патерн TopBar/StatusLine).
- Подивись, чи в раунді 7 плашку BTC прибирали з WaitingPage (коміт b79deb8 «Drop hero subtitle and price plate from waiting list») — якщо прибрали, поверни її саме в закріпленому вигляді за цим брифом. Розташування звір з макетом waiting-екрана в цьому ж Figma-файлі (знайди фрейм).

## Верифікація
- Playwright 390×844 (порт 3120): кожна зміна — скріншот проти скріншота Figma-вузла. Порожні дані/потрібні стани стаб через page.route (контракт DTO: src/services/dto/; УВАЖНО: у тікетах поле ownerId). Скрол-тест для п.4: проскроль список — плашка лишається на місці, фон суцільний без сітки.
- Усі 3 мови — тексти влазять (element.scrollWidth ≤ clientWidth).
- `npx tsc -b` чистий, `npm run build` зелений, lint не гірше базлайну (8 старих errors).
- Коміти: англійська, імператив, ≤70 символів, логічними групами.

## Звіт
Повний: `/Users/yuriikl/Code/its/Reack-and-Next/tttm/frontend-1.0/.sdd/round9-report.md`.
Фінальне повідомлення: статус, коміти, 1 рядок tsc/build/lint, розбіжності форми редагування (п.3) списком, concerns.
