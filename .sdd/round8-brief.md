# Раунд 8 — картка гри у списку за Figma 1360:10842

Репозиторій: `/Users/yuriikl/Code/its/Reack-and-Next/tttm/frontend-1.0` — React 19 + Vite + TS strict + Tailwind v4, працюєш прямо в main. Dev: `npm run dev -- --port 3120` (можливо вже запущений — перевір). Дані з моків, i18n — усі видимі тексти лише через переклади (`useT`; ключі сервляться з backend, але в dev є свій механізм — подивись як влаштовано в src/i18n/).

Джерело правди: Figma файл **`sjYaf8Zax1vT2STG6pJPMd`**, вузол **`1360:10842`** (картка гри в списку). get_design_context + get_screenshot; порівнюй з поточною `src/components/games/GameCard.tsx` (+ підкомпоненти, знайди їх у src/components/games/).

## П'ять змін (дослівно від користувача)
1. **«Зміна порядку дати та таймера до передбачення»** — місцями дата гри і таймер зворотного відліку до передбачення: порядок як у макеті.
2. **«Підказка про prediction pair в колі»** — у макеті є кругла підказка (іконка-коло) щодо валютної пари передбачення; додай точно за макетом (розмір, позиція, вигляд; якщо в макеті це інтерактивний hint — зроби доступно: button з aria-label, tooltip-поведінка за макетом; якщо статична іконка — статично).
3. **«Зміна TON на GRAM»** — валюта в картці: TON → GRAM (текстові підписи/суфікси сум; заміни в перекладах/формуттерах, НЕ хардкодь у JSX; перевір чи є formatter типу formatTon — переназви/додай обережно, не ламаючи інші екрани; якщо сума в картці ділить formatter з іншими місцями, де має лишитись TON — зроби параметрично).
4. **«Змінено відступи між елементами»** — звір усі внутрішні gap/padding картки з design context вузла і постав точно.
5. **«Індикатор кількості квитків поруч ціни квитка»** — біля ціни квитка додай індикатор кількості квитків, як у макеті (значення бери з моделі гри — подивись типи src/types/game.ts і моки; якщо поля немає — додай у view-модель/мок природно).

## Обовʼязкові правила
- Кольори/шрифти — токени проєкту (theme у src/styles/), нові хекси лише як нові токени.
- **Растрові асети з Figma — БЕЗ запеченого фону**: після download_assets перевір альфу кутів попіксельно (sharp є у сусідньому `/Users/yuriikl/Code/its/Reack-and-Next/tttm/website/node_modules/sharp`); якщо фон запечений — переекспортуй чистий вузол або flood-fill від кутів (толеранс ~14). Прості гліфи краще інлайн-SVG.
- Компонент ≤150 рядків; TypeScript strict без any.
- Не чіпай нічого поза карткою гри (GameCard і її підкомпоненти/типи/переклади/formatters).

## Верифікація
- Playwright 390×844: картка у списку (переконайся, що мок-дані дають хоча б одну гру; якщо список порожній — знайди як увімкнути мок-ігри) — скріншот проти скріншота Figma-вузла, звірка розмірів getBoundingClientRect проти design context (таблиця в звіті: елемент → Figma px → факт px).
- Усі 3 мови — тексти влазять, нічого не обрізається (element.scrollWidth ≤ clientWidth).
- `npx tsc -b` чистий, `npm run build` зелений, lint не гірше базлайну main (8 старих errors у нечіпаних файлах).
- Коміти: англійська, імператив, ≤70 символів, логічними групами.

## Звіт
Повний: `/Users/yuriikl/Code/its/Reack-and-Next/tttm/frontend-1.0/.sdd/round8-report.md`.
Фінальне повідомлення: статус (DONE/DONE_WITH_CONCERNS/NEEDS_CONTEXT/BLOCKED), коміти, 1 рядок tsc/build/lint, concerns.
